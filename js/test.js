/*
   var data={ "firstName" : "Hi Tom." };
   alert(data.firstName);
*/
/*
   var today = new Date();
   var hourNow = today.getHours();
   var minNow = today.getMinutes();

   var pelement = document.createElement("p");
   for (i = 0; i < 44; i++) {
      var textnode = document.createTextNode("\u00A0");
      pelement.appendChild(textnode);
      document.getElementById("hdr").appendChild(textnode);
   }

   if ((hourNow < 10) & (minNow < 10)) {
      var textnode = document.createTextNode("The time is 0" + hourNow + ":0" + minNow + "");
      pelement.appendChild(textnode);
      document.getElementById("hdr").appendChild(textnode);
   }
   else if (hourNow < 10) {
      var textnode = document.createTextNode("The time is 0" + hourNow + ":" + minNow + "");
      pelement.appendChild(textnode);
      document.getElementById("hdr").appendChild(textnode);
   }
   else if (minNow < 10) {
      var textnode = document.createTextNode("The time is " + hourNow + ":0" + minNow + "");
      pelement.appendChild(textnode);
      document.getElementById("hdr").appendChild(textnode);
   }
   else {
      var pelement = document.createElement("p");
      var textnode = document.createTextNode("The time is " + hourNow + ":" + minNow + "");
      pelement.appendChild(textnode);
      document.getElementById("hdr").appendChild(textnode);
   }
*/
